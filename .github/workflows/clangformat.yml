# ClangFormat
# Author: Eliot Hall
# This program formats all cpp files in the repository
name: ClangFormat

on:
  pull_request:
    branches: ["master"] 
  push:
    branches: ["master"]

jobs:
  format:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v3
      - name: Format .cpp and .hpp files
        run:
          clang-format ./src/*.cpp ./include/*.hpp -style=file:./.clang-format -i
      - name: Push to GitHub
        run: |
          git commit 
          git push origin